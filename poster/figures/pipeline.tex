% Vertical pipeline figure for the one-pager (was horizontal).
% 5 stages stacked top-to-bottom; arrows point down.
\begin{tikzpicture}[
    >=Stealth,
    box/.style={
      rectangle,
      rounded corners=3pt,
      draw=black!60,
      fill=blue!4,
      minimum height=1.05cm,
      minimum width=5.0cm,
      align=center,
      inner sep=4pt,
      text width=4.7cm,
      font=\small
    },
    arrow/.style={->, thick, draw=black!60, shorten <=2pt, shorten >=2pt, line width=0.8pt},
    node distance=0.35cm
  ]

  \node[box] (sources) {\textbf{Primary sources}\\[1pt]\footnotesize CCC, \emph{Summa}, Augustine};
  \node[box, below=of sources] (scrape)  {\textbf{Scrape}\\[1pt]\footnotesize Rate-limited HTTP + on-disk cache};
  \node[box, below=of scrape]  (clean)   {\textbf{Clean}\\[1pt]\footnotesize HTML\,$\to$\,structured JSONL chunks};
  \node[box, below=of clean]   (gen)     {\textbf{Distill}\\[1pt]\footnotesize Claude Sonnet 4.6 as teacher};
  \node[box, below=of gen]     (sft)     {\textbf{SFT}\\[1pt]\footnotesize Qwen 2.5\,7B, LoRA, MLX Q8};

  \draw[arrow] (sources) -- (scrape);
  \draw[arrow] (scrape)  -- (clean);
  \draw[arrow] (clean)   -- (gen);
  \draw[arrow] (gen)     -- (sft);

\end{tikzpicture}
