\begin{tikzpicture}[
    >=Stealth,
    box/.style={
      rectangle,
      rounded corners=3pt,
      draw=black!60,
      fill=blue!4,
      minimum height=2.1em,
      align=center,
      inner sep=5pt,
      text width=2.7cm,
      font=\small
    },
    arrow/.style={->, thick, draw=black!60},
    node distance=0.65cm
  ]

  \node[box] (sources) {Primary sources\\\footnotesize(CCC, Summa,\\Augustine)};
  \node[box, right=of sources] (scrape) {\texttt{scrape\_}\\\texttt{sources.py}};
  \node[box, right=of scrape]  (clean)  {\texttt{clean\_}\\\texttt{corpus.py}};
  \node[box, right=of clean]   (gen)    {\texttt{generate\_}\\\texttt{training\_pairs.py}\\\footnotesize(Claude Sonnet 4.6 teacher)};
  \node[box, right=of gen]     (sft)    {LoRA SFT\\\footnotesize Qwen 2.5\,7B\\MLX Q8};

  \draw[arrow] (sources) -- (scrape);
  \draw[arrow] (scrape)  -- (clean);
  \draw[arrow] (clean)   -- (gen);
  \draw[arrow] (gen)     -- (sft);

\end{tikzpicture}
